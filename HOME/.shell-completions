# ~/.shell-completions – load completions from ~/.shell-completions.d
[[ -n $DEBUG ]] && { PS4=':${LINENO}+'; set -x; }

[[ -d ~/.shell-completions.d ]] || return
fpath=(~/.shell-completions.d $fpath)
