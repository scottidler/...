#!/bin/bash

echo "starting agent..."
pkill ssh-agent
ssh-add -D
if [[ "`ssh backdoor1.awbdev.org 'ssh-add -l'`" == *"The agent has no identities."* ]]; then
    echo "failure"
    exit -1
fi
echo "success"
exit 0

