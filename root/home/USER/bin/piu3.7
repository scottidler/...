#!/bin/bash

if [ -n "$DEBUG" ]; then
    PS4=':${LINENO}+'
    set -x
fi

if hash pip3 2> /dev/null; then
    echo "updating pip3 packages..."
    PKGS="`pip3 list | tail -n +3 | cut -d' ' -f1 | xargs echo -n`"
    sudo -H python3.7 -m pip install --upgrade $PKGS
fi
