#!/bin/sh
while read line; do
    if [ -z "$TEXT" ]; then
        TEXT="$line\n"
    else
        TEXT="$TEXT$line\n"
    fi
done < "${1:-/dev/stdin}"
echo -e "$TEXT" | openssl req -in /dev/stdin -noout -text
