#!/bin/bash

if [ -n "$DEBUG" ]; then
    PS4=':${LINENO}+'
    set -x
fi

URLS=""
for ARG in "$@"; do
    if [ -f "$ARG" ]; then
        URLS+="$(cat "$ARG" | xargs echo -n) "
    else
        URLS+="$ARG "
    fi
done

CMD="firefox-trunk -private $URLS"
echo "$CMD"
$CMD
